doctype html
html(lang="en")
  head
    title Protocol Analyzer

  body
    p Select your probes
    div#ports
      each port in ports
        input(type="checkbox" value="#{port.comName}")
        label= port.comName
    button#start start
    div#workspace
    script(src='/js/bundle.js')
